module;

#include <cassert>

module anatomy.module;

Type function(Class *c) {
  assert(c != nullptr);
  return (data + foo()) * phi;
}

namespace Namespace {
double bar() { return foo(); }
} // namespace Namespace